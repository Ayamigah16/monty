#include "monty.h"
/**
 * stack_var - prints the top
 * @stack: stack
 * @line_number: line_number
 * Return: no return
*/
void stack_var(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
	bus.lifi = 0;
}
